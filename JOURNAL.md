# Journal

One entry per working session, appended, never edited. Newest at the bottom.

Two lines is a complete entry. If it takes ten, the session had more than one
topic — which is itself worth writing down.

The date heading is what `check-memory.sh` counts, so keep the format.

---

## 2026-08-17 — the component was extracted

- **Changed:** built from the working parts of a heavier mechanism it
  replaces — a nightly task that read finished conversations, driven by a
  registration queue and a bookmark log. Its readable input had shrunk to
  nothing, while the duty it served had not. What survives is the duty, one
  short rule, and a script that checks the rule instead of trusting it.
- **Open:** no remote yet; adoption in its first project is untested.
